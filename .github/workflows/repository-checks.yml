name: Repository integrity
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - name: Verify paths and preserved research artifacts
        run: python reproducibility/verify_repository.py
      - name: Prevent re-publication of suppressed municipal cells
        run: python reproducibility/test_publication_privacy.py
